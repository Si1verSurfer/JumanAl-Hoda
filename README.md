# goman_alhoda

Goman Al-Hoda — Flutter Islamic companion app (Arabic RTL).

## Setup

After cloning, install the large Quran mushaf assets (fonts + text data, ~55 MB):

```bash
./scripts/fetch_quran_assets.sh
flutter pub get
```

Then run the app as usual with `flutter run`.
