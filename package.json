{
  "name": "juman-alhoda-repo",
  "private": true,
  "description": "Monorepo root — website deploys from the website/ folder on Vercel.",
  "scripts": {
    "build:website": "npm run build --prefix website"
  }
}
