{
  "name": "juman-alhoda-repo",
  "private": true,
  "description": "Monorepo root. Vercel builds the website/ subdirectory.",
  "scripts": {
    "vercel-build": "npm install --prefix website && npm run build --prefix website",
    "build:website": "npm run build --prefix website"
  }
}
